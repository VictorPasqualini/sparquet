---
title: ¿Qué es Sparquet?
description: Un framework de PySpark orientado a configuración — describe el pipeline como un documento JSON, ejecútalo donde corra Spark y diséñalo en un lienzo.
sidebar:
  order: 1
  label: ¿Qué es Sparquet?
---

import { Aside, Card, CardGrid, Steps } from '@astrojs/starlight/components'

Sparquet son dos cosas que comparten un archivo.

**El framework** es una librería de Python que lee un pipeline descrito en JSON y lo ejecuta en Spark: lee una fuente, aplica transformaciones en orden, comprueba reglas de calidad y escribe uno o varios destinos.

**Sparquet Studio** es una aplicación de navegador que dibuja ese mismo JSON en un lienzo de nodos — y compila el lienzo de vuelta a él.

El documento es el contrato. El framework nunca necesita al Studio para ejecutar un trabajo, y el Studio nunca inventa sintaxis que el framework no soporte.

## La idea completa en un archivo

```json title="pedidos_curados.json"
{
  "name": "pedidos_curados",
  "input": {
    "format": "postgres",
    "path": "public.pedidos",
    "options": { "host": "db.interno", "database": "ventas", "password_env": "PG_PASSWORD" }
  },
  "transformations": [
    { "type": "filter", "condition": "status = 'CONFIRMADO'" },
    { "type": "with_column", "column": "ingresos", "expression": "cantidad * precio_unitario" }
  ],
  "validations": {
    "on_failure": "warn",
    "rules": [{ "type": "not_null", "columns": ["id"] }]
  },
  "output": { "format": "delta", "path": "analytics.pedidos", "mode": "overwrite" }
}
```

```python
from spark_framework import SparkFramework

fw = SparkFramework()
print(fw.run("pedidos_curados.json").summary())
fw.stop()
```

Esa es toda la superficie: un archivo y una llamada. Todo lo demás en esta documentación es un campo que puedes añadir a ese archivo.

## Por qué configuración en vez de código

<CardGrid>
  <Card title="Revisable">
    El pipeline es un documento. Cabe en un pull request, se compara limpio
    entre entornos y no se esconde en el estado de un cuaderno.
  </Card>
  <Card title="Reutilizable">
    Los marcadores `{param}` convierten un archivo en muchas ejecuciones — por
    región, por fecha, por cliente — sin copiar lógica.
  </Card>
  <Card title="Consistente">
    Lecturas, escrituras, merges y comprobaciones de calidad se comportan igual
    en cada trabajo, porque son el mismo camino de código.
  </Card>
  <Card title="Visual cuando sirve">
    El lienzo es un editor del archivo, no un reemplazo. Elimina el Studio y
    todos los trabajos siguen funcionando.
  </Card>
</CardGrid>

## Lo que no es

- **No es un orquestador.** Sparquet ejecuta un pipeline. Airflow, Dagster, Databricks Workflows o cron deciden *cuándo*.
- **No es una plataforma alojada.** No hay plano de control y nada llama a casa. Es una librería en tu clúster.
- **No reemplaza a Spark.** Es una forma de escribir trabajos de Spark que no tendrás que reescribir. Lo que el framework no expresa, lo cubren la transformación `sql` y los registros propios.

## Cómo está organizada esta documentación

<Steps>

1. **[Instalación](/es/docs/start/install/)** — el framework y, opcionalmente, el Studio.

2. **[Tu primer pipeline](/es/docs/start/quickstart/)** — un trabajo funcionando en cinco minutos, desde la terminal.

3. **Tu primer flujo en el Studio** — el mismo pipeline, dibujado en el lienzo.

4. **Conceptos** — orden de ejecución y cómo el grafo se convierte en el archivo.

5. **Referencia** — cada transformación, conector, validador y API, campo a campo.

6. **Guías** — recetas de principio a fin: bases de datos, joins con pushdown, calidad de datos, cargas incrementales.

</Steps>

<Aside type="tip" title="¿Con prisa?">
  Ve directo a [Tu primer pipeline](/es/docs/start/quickstart/). Son cinco
  minutos y el resultado es un conjunto de datos Parquet real en disco.
</Aside>

<Aside type="note" title="Idiomas">
  La referencia completa está en inglés. Las páginas todavía sin traducir se
  muestran automáticamente en inglés.
</Aside>
