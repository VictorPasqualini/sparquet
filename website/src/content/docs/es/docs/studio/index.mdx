---
title: Sparquet Studio
description: El editor visual de pipelines Sparquet — qué es, cómo guarda tu trabajo y cómo mapea un canvas al JSON que el framework ejecuta.
sidebar:
  order: 1
  label: Visión general
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components'

Studio es una aplicación de navegador que edita pipelines Sparquet en un canvas de nodos. No es un producto aparte con su propio formato: lee el JSON del pipeline y escribe el JSON del pipeline.

```bash
cd sparquet-studio
npm install
npm run dev          # http://localhost:5273
```

<CardGrid>
  <Card title="Local first">
    Los proyectos y workflows viven en el IndexedDB de tu navegador. Sin cuenta,
    sin servidor, sin telemetría. Exporta todo el workspace como un JSON.
  </Card>
  <Card title="Sin lock-in">
    El canvas compila al archivo que corre la CLI. Borra Studio y cada pipeline
    sigue funcionando.
  </Card>
  <Card title="Documentado en su lugar">
    Cada campo lleva las mismas notas que esta referencia, incluidos los
    comportamientos que normalmente viven solo en el código fuente del framework.
  </Card>
  <Card title="Runner opcional">
    Apúntalo a un pequeño servicio local y ejecuta pipelines desde el canvas, con
    contadores, validaciones y un preview de los datos.
  </Card>
</CardGrid>

## El layout

| Área | Qué hace |
|---|---|
| **Palette** (izquierda) | Toda fuente, transformación, validator y destino. Arrastra al canvas, o haz clic para añadir tras el último nodo. |
| **Canvas** (centro) | El pipeline como nodos conectados. De izquierda a derecha es el orden de ejecución. |
| **Inspector** (derecha) | Los campos del nodo seleccionado, con ayuda, ejemplos y gotchas. |
| **JSON panel** | El pipeline compilado, en vivo. También la superficie de import. |
| **Issues panel** | Resultados de lint mientras escribes, cada uno enlazando el nodo y el campo. |
| **Run panel** | Ejecución mediante el runner local. |
| **Status bar** | Conteo de nodos, validez, estado de guardado. |

Los paneles comparten una columna a la derecha, en pestañas y redimensionable — <kbd>Ctrl/⌘</kbd>+<kbd>J</kbd>, <kbd>Ctrl/⌘</kbd>+<kbd>E</kbd>, <kbd>Ctrl/⌘</kbd>+<kbd>/</kbd> y <kbd>Ctrl/⌘</kbd>+<kbd>Enter</kbd> alternan entre ellos.

## Proyectos y workflows

Un **workflow** es un pipeline. Un **proyecto** agrupa workflows relacionados — normalmente uno por dominio: `Sales`, `Billing`, `CRM`.

Ambos viven en tu navegador. **Settings → Data** exporta todo como un único bundle JSON y lo reimporta, que es como mueves un workspace entre máquinas o compartes un conjunto de pipelines con un colega.

<Aside type="caution">
  El almacenamiento del navegador es por navegador y por perfil. Borrar los datos
  del sitio elimina tu workspace. Commitea los pipelines compilados a git — son el
  artefacto que importa, y Studio puede reimportar cualquiera de ellos.
</Aside>

## Cómo el canvas se vuelve el archivo

Dos reglas definen todo el mapeo:

**La cadena compartida es el pipeline principal.** Lo que todo destino tiene en común se vuelve el `transformations` de nivel superior. Después de que el grafo se ramifica, cada rama se vuelve el `transformations` de ese destino.

**Un join o union toma su segunda fuente de su segundo input.** La cadena que alimenta ese handle se vuelve `with` más `with_transformations`.

Todo lo demás es un campo de nodo. Las sticky notes nunca compilan; los nodos silenciados se saltan.

<Aside type="tip">
  Pulsa <kbd>Ctrl/⌘</kbd>+<kbd>J</kbd> en cualquier momento para ver exactamente
  lo que produce tu dibujo. Si el JSON no es lo que esperabas, el grafo no es lo
  que pensabas — el panel es la forma más rápida de aprender el mapeo.
</Aside>

## Importar lo que ya tienes

Abre el panel JSON, cambia a **Edit**, pega un pipeline y haz clic en **Apply to canvas**. Studio lo organiza automáticamente. Los tipos de transformación desconocidos — los personalizados que registraste — se preservan intactos en import y export.

## Verificar un cambio

Studio trae sus propias comprobaciones:

```bash
npm run typecheck   # TypeScript, cero errores esperados
npm run test        # tests unitarios, incluidos round-trips del compiler
npm run lint        # eslint
npm run smoke       # pasada end-to-end en un Chrome real
```

El compiler está cubierto por tests de round-trip sobre los pipelines de ejemplo del propio framework: importa cada uno, compila de vuelta y compara. Ese test es lo que garantiza que el canvas nunca cambie tu archivo en silencio.

## Siguiente

- [El canvas](/es/docs/studio/canvas/) — conexiones, branching, control por teclado
- [El inspector](/es/docs/studio/inspector/) — editar campos de nodos
- [Asistente de IA](/es/docs/studio/ai-assistant/) — generar y modificar pipelines
- [Ejecutar pipelines](/es/docs/studio/running/) — el runner local
- [Plantillas y lecciones](/es/docs/studio/templates/) — la forma más rápida de aprender una función
