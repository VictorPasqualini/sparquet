{
  "name": "formatos-csv-escrita",
  "description": "Escreve o seed em csv. Metade de ida do round-trip executado.",
  "input": {
    "format": "csv",
    "path": "{dir}/seed.csv",
    "options": {
      "header": "true",
      "inferSchema": "true"
    }
  },
  "transformations": [
    {
      "type": "filter",
      "condition": "valor IS NULL OR valor > -1000"
    }
  ],
  "output": {
    "format": "csv",
    "path": "{dir}/out-csv",
    "mode": "overwrite",
    "options": {
      "header": "true"
    }
  }
}
