# Sparquet Studio local execution bridge.
#
# pyspark and the sparquet framework itself are NOT listed here on purpose:
# they come from the parent repository (run this service from the repo root, or
# install the framework into the same environment). Install those with the
# repository's own requirements.txt / `pip install -e .`.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
